// 拼接头像Url函数
var concatAvatarUrl = function (avatarUrl, imageUrl) {
  var url = ''
  if (avatarUrl) {
    url = imageUrl + avatarUrl
  } else {
    url = '../../images/avatar.png'
  }
  return url
}

module.exports.concatAvatarUrl = concatAvatarUrl
